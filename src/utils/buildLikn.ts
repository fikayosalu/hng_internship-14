// const buildLink = (queryParam:Record<string, any>, page: number, limit: number) => {
//   const params = new URLSearchParams();

//   for (const [key, value] of Object.entries(queryParam)) {
//     if (key !== "page" || key !== ""
//   }

// };
